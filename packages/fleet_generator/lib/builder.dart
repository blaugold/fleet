import 'package:build/build.dart';

import 'src/fleet_builder.dart';

/// Builder that does all the code generation supported by Fleet.
Builder fleetBuilder(BuilderOptions options) => FleetBuilder();
