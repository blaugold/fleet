name: fleet_example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  fleet:
    path: ..
  fleet_imports:
    path: ../../fleet_imports
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
